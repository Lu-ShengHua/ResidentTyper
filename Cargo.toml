[package]
name = "resident_typer_demo"
version = "1.4.0"
edition = "2021"
build = "build.rs"

[profile.release]
opt-level = "z"
strip = true
lto = true

[dependencies]
windows = { version = "0.58.0", features = [
    "Win32_Foundation",
    "Win32_System_Com",
    "Win32_UI_Accessibility",
] }
